    V := data { a, b, c, d, e, f, g, h } ;
 
    A := data { ( a, b ), ( a, c ), ( b, d ), ( b, e ), ( c, g ), ( d, f ), ( e, f ), ( f, h ), ( g, h ) } ;
 
 
    cap := data
    { ( a, b ) : 25,  ( a, c ) : 15,  ( b, d ) : 10,  ( b, e ) : 10,  ( c, g ) : 10,  ( d, f ) : 15,  ( e, f ) : 15,  ( f, h ) : 25,
  ( g, h ) : 15 } ;
 
    K := data { ( a, b ), ( a, f ), ( a, h ), ( c, g ) } ;
 
    phi := data { ( a, b ) : 15,  ( a, f ) : 10,  ( a, h ) : 5,  ( c, g ) : 5 } ;
 
    sf_count := data { } ;
 


    y := data { ( a, b, a, b ) : 1,  ( b, d, a, f ) : 1,  ( c, g, a, h ) : 1,  ( c, g, c, g ) : 1 } ;
 
 
    f := data
    { ( a, b, a, b ) : 15,  ( a, b, a, f ) : 10,  ( a, c, a, h ) :  5,  ( b, d, a, f ) : 10,  ( c, g, a, h ) :  5,
      ( c, g, c, g ) :  5,  ( d, f, a, f ) : 10,  ( g, h, a, h ) :  5 } ;
 
    spliting_node := data { } ;
 
    sf_share := data { } ;
 
    rcv_int := data { ( a, b ) : 1,  ( a, f ) : 1,  ( a, h ) : 1,  ( c, g ) : 1 } ;
 
    sf_count := data { } ;
 
    FlowSUM := 35 ;
 
    sf_link_share := data { } ;
 
    sf_bneck := data { } ;
 
    sf_bneck_count := data { } ;
 
    z := data { } ;
 
