    V := data { a, b, c, d, e, f, g, h } ;
 
    A := data { ( a, b ), ( a, c ), ( b, d ), ( b, e ), ( c, g ), ( d, f ), ( e, f ), ( f, h ), ( g, h ) } ;
 
 
    cap := data
    { ( a, b ) : 25,  ( a, c ) : 15,  ( b, d ) : 10,  ( b, e ) : 10,  ( c, g ) : 20,  ( d, f ) : 15,  ( e, f ) : 15,  ( f, h ) : 25,
  ( g, h ) : 15 } ;
 
    K := data { ( a, h ), ( c, h ) } ;
 
    phi := data { ( a, h ) : 10,  ( c, h ) : 15 } ;
 
    sf_count := data { } ;
 


    y := data { ( b, d, a, h ) : 1,  ( g, h, c, h ) : 1 } ;
 
 
    f := data
    { ( a, b, a, h ) : 10,  ( b, d, a, h ) : 10,  ( c, g, c, h ) : 15,  ( d, f, a, h ) : 10,  ( f, h, a, h ) : 10,  ( g, h, c, h ) :
 15 } ;
 
    spliting_node := data { } ;
 
    sf_share := data { } ;
 
    rcv_int := data { ( a, h ) : 1,  ( c, h ) : 1 } ;
 
    sf_count := data { } ;
 
    FlowSUM := 25 ;
 
    sf_link_share := data { } ;
 
    sf_bneck := data { } ;
 
    sf_bneck_count := data { } ;
 
    z := data { } ;
 
