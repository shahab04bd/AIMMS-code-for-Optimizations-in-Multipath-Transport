    V := data { a, b, c, d, e, f, m, n, g, h, i, j, k, l } ;
 
    A := data { ( a, b ), ( a, d ), ( b, c ), ( c, f ), ( c, n ), ( d, e ), ( e, f ), ( e, l ), ( m, b ), ( i, d ), ( i, j ), ( j, k
 ), ( k, l ) } ;
 
 
    cap := data table
    b   c   d   e   f   n   j   k   l
!  --  --  --  --  --  --  --  --  --
a  20      20
b      25
c                  20  35
d              20
e                  15              30
m  30
i          40              30
j                              15
k                                  20
    ;
 
 
    K := data { ( a, c ), ( a, f ), ( m, n ), ( i, e ), ( i, l ) } ;
 
    phi := data { ( a, c ) : 12.500,  ( a, f ) : 10.000,  ( m, n ) : 12.500,  ( i, e ) : 10.000,  ( i, l ) : 15.000 } ;
 
    sf_count := data { } ;
 


    y := data { ( b, c, a, c ) : 1,  ( b, c, m, n ) : 1,  ( d, e, a, f ) : 1,  ( d, e, i, e ) : 1,  ( j, k, i, l ) : 1 } ;
 
 
    f := data
    { ( a, b, a, c ) : 12.500,  ( a, d, a, f ) : 10.000,  ( b, c, a, c ) : 12.500,  ( b, c, m, n ) : 12.500,
      ( c, n, m, n ) : 12.500,  ( d, e, a, f ) : 10.000,  ( d, e, i, e ) : 10.000,  ( e, f, a, f ) : 10.000,
      ( m, b, m, n ) : 12.500,  ( i, d, i, e ) : 10.000,  ( i, j, i, l ) : 15.000,  ( j, k, i, l ) : 15.000,
      ( k, l, i, l ) : 15.000 } ;
 
    spliting_node := data { } ;
 
    sf_share := data { } ;
 
    rcv_int := data { ( a, c ) : 1,  ( a, f ) : 1,  ( m, n ) : 1,  ( i, e ) : 1,  ( i, l ) : 1 } ;
 
    sf_count := data { } ;
 
    FlowSUM := 60 ;
 
    sf_link_share := data { ( a, d, a, f ) : 20,  ( e, l, i, l ) : 30,  ( i, j, i, l ) : 30,  ( k, l, i, l ) : 20 } ;
 
    sf_bneck := data { } ;
 
    sf_bneck_count := data { } ;
 
    z := data { } ;
 
