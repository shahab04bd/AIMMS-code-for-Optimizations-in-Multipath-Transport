    V := data { a, b, c, d, e, f, m, n, g, h, i, j, k, l } ;
 
    A := data { ( a, b ), ( a, d ), ( b, c ), ( c, f ), ( c, n ), ( d, e ), ( e, f ), ( e, l ), ( m, b ), ( i, d ), ( i, j ), ( j, k
 ), ( k, l ) } ;
 
 
    cap := data table
    b   c   d   e   f   n   j   k   l
!  --  --  --  --  --  --  --  --  --
a  20      20
b      25
c                  20  15
d              20
e                  15              30
m  10
i          10              10
j                              15
k                                  20
    ;
 
 
    K := data { ( a, c ), ( a, f ), ( m, n ), ( i, e ), ( i, l ) } ;
 
    phi := data { ( a, c ) : 15,  ( a, f ) : 10,  ( m, n ) : 10,  ( i, e ) : 10,  ( i, l ) : 10 } ;
 
    sf_count := data { } ;
 


    y := data { ( b, c, a, c ) : 1,  ( d, e, a, f ) : 1,  ( m, b, m, n ) : 1,  ( i, d, i, e ) : 1,  ( i, j, i, l ) : 1 } ;
 
 
    f := data
    { ( a, b, a, c ) : 15,  ( a, d, a, f ) : 10,  ( b, c, a, c ) : 15,  ( b, c, m, n ) : 10,  ( c, n, m, n ) : 10,
      ( d, e, a, f ) : 10,  ( d, e, i, e ) : 10,  ( e, f, a, f ) : 10,  ( m, b, m, n ) : 10,  ( i, d, i, e ) : 10,
      ( i, j, i, l ) : 10,  ( j, k, i, l ) : 10,  ( k, l, i, l ) : 10 } ;
 
    spliting_node := data { } ;
 
    sf_share := data { } ;
 
    rcv_int := data { ( a, c ) : 1,  ( a, f ) : 1,  ( m, n ) : 1,  ( i, e ) : 1,  ( i, l ) : 1 } ;
 
    sf_count := data { } ;
 
    FlowSUM := 55 ;
 
    sf_link_share := data { ( a, d, a, f ) : 20,  ( c, n, m, n ) : 15,  ( e, l, i, l ) : 30,  ( j, k, i, l ) : 15,  ( k, l, i, l ) :
 20 } ;
 
    sf_bneck := data { } ;
 
    sf_bneck_count := data { } ;
 
    z := data { } ;
 
