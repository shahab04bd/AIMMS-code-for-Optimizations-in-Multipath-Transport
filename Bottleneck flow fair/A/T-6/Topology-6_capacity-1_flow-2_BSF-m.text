    V := data { a, b, c, d, e, f, i, j, k, l, m, n } ;
 
    A := data { ( a, b ), ( a, d ), ( b, c ), ( c, f ), ( d, e ), ( e, f ), ( f, i ), ( i, j ), ( i, l ), ( j, k ), ( k, n ), ( l, m
 ), ( m, n ) } ;
 
 
    cap := data table
     b    c    d    e    f    i    j    k    l    m    n
!  ---  ---  ---  ---  ---  ---  ---  ---  ---  ---  ---
a  100       100
b        50
c                      100
d                  10
e                      100
f                            30
i                                500       500
j                                      10
k                                                    500
l                                               200
m                                                    500
    ;
 
 
    K := data { ( a, f ), ( a, i ), ( f, n ), ( i, n ) } ;
 
    phi := data { ( a, f ) : 50,  ( a, i ) : 10,  ( f, n ) : 10,  ( i, n ) : 200 } ;
 
    sf_count := data { } ;
 


    y := data { ( b, c, a, f ) : 1,  ( d, e, a, i ) : 1,  ( j, k, f, n ) : 1,  ( l, m, i, n ) : 1 } ;
 
 
    f := data
    { ( a, b, a, f ) :  50,  ( a, d, a, i ) :  10,  ( b, c, a, f ) :  50,  ( c, f, a, f ) :  50,  ( d, e, a, i ) :  10,
      ( e, f, a, i ) :  10,  ( f, i, a, i ) :  10,  ( f, i, f, n ) :  10,  ( i, j, f, n ) :  10,  ( i, l, i, n ) : 200,
      ( j, k, f, n ) :  10,  ( k, n, f, n ) :  10,  ( l, m, i, n ) : 200,  ( m, n, i, n ) : 200 } ;
 
    spliting_node := data { } ;
 
    sf_share := data { } ;
 
    rcv_int := data { ( a, f ) : 1,  ( a, i ) : 1,  ( f, n ) : 1,  ( i, n ) : 1 } ;
 
    sf_count := data { } ;
 
    FlowSUM := 270 ;
 
 
    sf_link_share := data
    { ( a, b, a, f ) : 100,  ( a, b, a, i ) : 100,  ( a, d, a, f ) : 100,  ( a, d, a, i ) : 100,  ( c, f, a, f ) : 100,
      ( c, f, a, i ) : 100,  ( e, f, a, f ) : 100,  ( e, f, a, i ) : 100,  ( i, j, f, n ) : 500,  ( i, j, i, n ) : 500,
      ( i, l, f, n ) : 500,  ( i, l, i, n ) : 500,  ( k, n, f, n ) : 500,  ( k, n, i, n ) : 500,  ( m, n, f, n ) : 500,
      ( m, n, i, n ) : 500 } ;
 
    sf_bneck := data { } ;
 
    sf_bneck_count := data { } ;
 
    z := data { } ;
 
