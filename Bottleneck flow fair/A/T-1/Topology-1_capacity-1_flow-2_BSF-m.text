    V := data { a, b, c, d, e, f, m, n } ;
 
    A := data { ( a, b ), ( a, d ), ( b, c ), ( c, f ), ( c, n ), ( d, e ), ( e, f ), ( m, b ) } ;
 
    cap := data { ( a, b ) : 20,  ( a, d ) : 20,  ( b, c ) : 25,  ( c, f ) : 15,  ( c, n ) : 15,  ( d, e ) : 10,  ( e, f ) : 15,  (
m, b ) : 10 } ;
 
    K := data { ( a, c ), ( a, f ), ( m, n ) } ;
 
    phi := data { ( a, c ) : 15,  ( a, f ) : 10,  ( m, n ) : 10 } ;
 
    sf_count := data { } ;
 


    y := data { ( b, c, a, c ) : 1,  ( d, e, a, f ) : 1,  ( m, b, m, n ) : 1 } ;
 
 
    f := data
    { ( a, b, a, c ) : 15,  ( a, d, a, f ) : 10,  ( b, c, a, c ) : 15,  ( b, c, m, n ) : 10,  ( c, n, m, n ) : 10,
      ( d, e, a, f ) : 10,  ( e, f, a, f ) : 10,  ( m, b, m, n ) : 10 } ;
 
    spliting_node := data { } ;
 
    sf_share := data { } ;
 
    rcv_int := data { ( a, c ) : 1,  ( a, f ) : 1,  ( m, n ) : 1 } ;
 
    sf_count := data { } ;
 
    FlowSUM := 35 ;
 
    sf_link_share := data { ( a, d, a, f ) : 20,  ( c, n, m, n ) : 15,  ( e, f, a, f ) : 15 } ;
 
    sf_bneck := data { } ;
 
    sf_bneck_count := data { } ;
 
    z := data { } ;
 
